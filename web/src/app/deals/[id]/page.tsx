import { SectionPlaceholder } from "@/components/placeholder";

export default function Page() {
  return (
    <SectionPlaceholder title="Overview" description="Section cards with headline metrics appear here once a run completes." />
  );
}
